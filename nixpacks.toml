[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install --prefix backend",
  "npm install --include=dev --prefix frontend"
]

[phases.build]
cmds = ["npm run build --prefix frontend"]
